name: tezos_wallet
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  tezster_dart: 
    git: https://github.com/SushantChandla/Tezster_dart.git
  rxdart: ^0.27.1
  http: ^0.13.3
  shared_preferences: ^2.0.6
  qr_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  flutter_launcher_icons: 

flutter_icons:
  android: true
  ios: true
  image_path: "assets/wally_logo.png"

flutter:

  uses-material-design: true

  assets:
    - assets/
